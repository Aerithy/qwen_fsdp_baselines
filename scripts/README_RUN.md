# Run notes

The launch scripts assume a single-node multi-GPU setup and use torchrun.
They read hyperparameters from the YAML configs.

Before running, update dataset_name_or_path in the config file.
